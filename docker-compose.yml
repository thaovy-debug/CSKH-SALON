services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: linhkienled1000-app
    restart: unless-stopped
    ports:
      - "3020:3000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
      LINHKIENLED1000_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/chromium
      ZALO_RUNTIME_DIR: /app/data-runtime
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/api/health').then(r => { if (!r.ok) process.exit(1) }).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3
    volumes:
      - whatsapp_auth:/app/.wwebjs_auth
      - zalo_runtime:/app/data-runtime
    deploy:
      resources:
        limits:
          memory: 2G

  db:
    image: postgres:16-alpine
    container_name: linhkienled1000-db
    restart: unless-stopped
    profiles: ["local-db"]
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: linhkienled1000
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d linhkienled1000"]
      interval: 5s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 512M

volumes:
  postgres_data:
  whatsapp_auth:
  zalo_runtime:
